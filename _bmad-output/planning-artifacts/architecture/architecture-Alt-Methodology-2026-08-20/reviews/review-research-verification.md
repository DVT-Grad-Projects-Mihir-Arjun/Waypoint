# Research Verification Review — Waypoint

## Overall verdict
Every committed technology claim in v0.4 checks out against current, externally-verified facts, and the round's removal of the git-trailer / CI push-back mechanisms left no orphaned or contradictory references anywhere in the document. No new technology-currency claim was introduced beyond the git-host bypass-audit-log claim, which is accurate for both GitHub and GitLab. No findings to report.

## Findings
None.

## Verified current
- **Tech Stack table unchanged** (§ Tech Stack): Confirmed the table still reads Node.js 22+, Commander.js, Vitest, Husky (or Lefthook), TypeScript, YAML, Markdown+frontmatter, npm distribution — byte-for-byte consistent with a stale-copy check against the described baseline; no edits this round.
- **Node.js 22+ floor, LTS reasoning** (§ Tech Stack): Verified against current Node.js release data — Node 20 reached end-of-life April 30, 2026; Node 22 is Maintenance LTS (EOL April 30, 2027); Node 24 is Active LTS (EOL April 30, 2028, active since Oct 2025). The document's rationale ("22 is the current Maintenance LTS line; 24 is Active LTS but newer") is accurate as written. (Minor, not flagged as a finding: the phrase "Node 20 reaches end-of-life April 2026" is now past-tense-appropriate since that date has passed as of the document's own 2026-08-20 dating — a cosmetic tense nit, not a factual error, and not worth a severity-rated finding.)
- **Commander.js "most-downloaded Node CLI framework" claim** (§ Tech Stack): Confirmed current — Commander remains the highest-download CLI parsing library in the Node ecosystem (tens of millions of weekly downloads, ahead of yargs and far ahead of oclif), and it is zero-dependency, matching the stated rationale.
- **Vitest "current standard for new TS/JS projects" claim** (§ Tech Stack): Confirmed current — Vitest is the recommended default for new JS/TS projects in 2026 and has overtaken Jest in downloads for new-project starts, while Jest remains common in legacy codebases (consistent with the document only claiming Vitest is standard for *new* projects).
- **Husky / Lefthook framing** (§ Tech Stack): Confirmed current — Husky remains the dominant, actively-maintained Node git-hook manager; Lefthook (Go binary, no Node dependency) remains a real, actively-maintained lighter-weight alternative, matching the document's framing exactly (Husky as MVP default, Lefthook as viable alternative for non-Node repos).
- **No orphaned references to removed mechanisms**: Searched the full document for any live (non-changelog) reference to `git interpret-trailers`, the `Waypoint-Task:` trailer, a post-commit hook, an async check-command execution model, or a CI push-back/write path. None found — the only mentions of these removed mechanisms are in the historical Change Log entries for v0.3/v0.4, which correctly describe them in the past tense as replaced. The current body (Data Models, Components, Core Workflows, Infrastructure, Error Handling) consistently and exclusively describes the new `waypoint verify` command and read-only CI check with no stale cross-reference. The one place `contents: write` is mentioned in the live text (§ Infrastructure) is a deliberate contrast — "CI needs no `contents: write`, no push access" — not a leftover claim that the old mechanism still exists.
- **No new technology-currency claim beyond the bypass-audit claim**: Skimmed the full v0.4 diff area (Components → Gate Script, Core Workflows → Feature-tier flow, Error Handling → Bypass) for any newly-named tool, library, or platform-specific API. Found none — the redesign reuses the already-vetted stack (TypeScript, Commander.js, the existing `gate()`/`runCheck()` functions) and introduces no new dependency. The only fact requiring external verification is the git-host bypass-audit claim addressed below.
- **"GitHub's 'merge without waiting for requirements,' GitLab's equivalent" claim** (§ Core Workflows → Bypass, reframed; § Error Handling Strategy → Bypass): Confirmed accurate and current on both platforms. GitHub: repo admins with the setting enabled see a literal "Merge without waiting for requirements to be met (bypass branch protections)" option, and GitHub's audit log records this as a `protected_branch.policy_override` event, capturing who performed it and when. GitLab: merge-request approval overrides and bypassed-policy merges are recorded as project-level audit events, likewise capturing who and when. The document's claim that "every mainstream git host... already logs natively, tied to who did it and when" holds for both named platforms.
