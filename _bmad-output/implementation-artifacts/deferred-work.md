# Deferred Work

Findings from code review that are real but out of scope for their source story — not bugs to fix now, but risks or gaps worth tracking for a future story or a deliberate follow-up decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: `packages/cli/package.json` and `packages/core/package.json` lack standard metadata fields (`description`, `license`, `repository`, `author`).
  evidence: Neither file was reviewed against npm packaging conventions during Story 1.1 since the story's scope was scaffold behavior, not package publishing readiness; matters once these packages are actually published to a registry.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: `packages/cli/package.json` pins `@waypoint/core` to an exact version (`0.1.0`) rather than a caret range, so `packages/core` bug fixes won't reach installed CLIs without a manual `cli` version bump and republish.
  evidence: Confirmed by reading `packages/cli/package.json`; within an npm workspace this doesn't block local development, but it's a real distribution-time risk once the packages are published independently rather than resolved via the workspace.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: `ensureGitignoreEntry` (`packages/core/src/gitignore.ts`) matches `.gitignore` lines by exact string equality, so it cannot recognize that a broader existing pattern (e.g. a bare `.waypoint/`) already covers the entry it's about to add, and will append a redundant line.
  evidence: Read the function directly — it does `content.split(/\r?\n/).some((line) => line.trim() === entry)`, which is correct for the story's literal requirement ("append if it doesn't already contain it") but not for pattern-aware `.gitignore` semantics; no functional bug today, just imprecise duplicate-detection, flagged in case a future story wants stricter gitignore hygiene.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: The root `.gitignore`'s `/node_modules/` entry is anchored to the repo root only, so it won't ignore `packages/*/node_modules/` if a future contributor uses a package manager or workflow that creates per-package `node_modules` directories instead of npm workspaces' hoisted layout.
  evidence: Confirmed by reading `.gitignore` — the entry is `/node_modules/` (leading slash, root-anchored), not the common recursive `node_modules/` pattern; not a problem under the current npm-workspaces setup, but a latent gap if the tooling ever changes.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: The Waypoint monorepo itself has no root `README.md` or `LICENSE` file.
  evidence: Outside Story 1.1's scope (which scaffolds a *consuming* repo's structure, not this repo's own documentation), but a real gap before any public distribution of the `waypoint` package.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: `checkConflict` in `packages/core/src/scaffold.ts` follows symlinks via `statSync` without special-casing them, so a scaffolded path that is a symlink to a directory elsewhere (including outside the repo) is silently accepted and written through rather than flagged as an unusual pre-existing state.
  evidence: Read `checkConflict` directly — it calls `existsSync`/`statSync`, both of which follow symlinks by default, with no `lstatSync` check; not a security boundary this story promised to defend (the I/O matrix doesn't mention symlinks), but worth a deliberate decision later on whether symlinked scaffold paths should be accepted, warned on, or rejected.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-install-scaffolds-the-repo-structure.md`
  summary: If a transient write failure (e.g. `EACCES`, disk full) occurs partway through the plan-entry write loop in `scaffold()`, already-created paths from that same run are left in place with no rollback — a retry will report them as "kept" rather than the run being fully atomic.
  evidence: Read `scaffold()`'s main loop directly — each entry is written independently with no compensating cleanup on a later entry's failure; the story's "no partial writes" guarantee (I/O matrix, Path collision row) is about pre-flight conflict detection preventing partial writes on a *known* conflict, not about recovering from a mid-write I/O failure, so this is a narrower, lower-probability gap rather than a spec violation.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-patch-tier-spec-creation.md`
  summary: `createPatchSpec`'s name validation and cross-tier collision check don't account for case-insensitive filesystems (macOS APFS, Windows), so `Fix-Typo` and `fix-typo` are treated as distinct valid names by the regex and collision check even though they resolve to the same file on those platforms.
  evidence: Confirmed by reading `new-spec.ts`'s `NAME_PATTERN` and the `SPEC_TIERS` collision loop, both of which use plain string equality/regex matching with no case-folding; not something a mechanical patch should decide unilaterally, since fixing it requires a deliberate naming-policy call (reject mixed case? lowercase-normalize names? case-insensitive collision check only?) rather than a one-line code change.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-patch-tier-spec-creation.md`
  summary: `createPatchSpec`'s name validation regex (`/^[a-zA-Z0-9][a-zA-Z0-9_-]*$/`) accepts Windows-reserved device basenames (`con`, `aux`, `nul`, `com1`, etc.), which would fail unpredictably when writing `specs/patches/<name>.md` on Windows.
  evidence: Confirmed by reading the regex directly — none of the reserved names are excluded; a real but niche cross-platform gap, and whether to add a reserved-name blocklist is a deliberate scope/policy decision for a future story rather than a mechanical fix to this one.

- source_spec: none
  summary: `waypoint new-system <name>` (System-tier spec-set scaffolding — PRD-style requirements, an architecture stub, ADR stub(s), and phased tasks) was split out of Story 1.3 and deferred to its own future spec.
  evidence: Story 1.3 as written in epics.md bundles `new-feature` (a single spec file + matching ledger, closely reusing Story 1.2's pattern) with `new-system` (a materially new multi-file spec-set layout with its own ledger/phased-task design questions not resolved by the epics.md AC alone). Per the SCOPE STANDARD's multi-goal criteria, these are two independently shippable deliverables; the human chose to split and build `new-feature` first, deferring `new-system` to a dedicated follow-up spec.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-feature-tier-spec-creation.md`
  summary: `createPatchSpec`/`createFeatureSpec`'s exclusive-create (`'wx'`) writes protect against a same-command race on the same `<name>`, but not a cross-command race — e.g. `waypoint new-patch foo` and `waypoint new-feature foo` running concurrently could both pass their respective collision checks before either writes, since neither checks the other's target path atomically with a shared lock.
  evidence: Confirmed by reading both functions directly — each only exclusive-creates its own target file(s); nothing coordinates across the two functions or CLI invocations. Closing this fully would need a shared lock mechanism (like `scaffold.ts`'s advisory lock directory) spanning all `new-*` commands, which is a deliberate concurrency-design decision rather than a mechanical patch to one function.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-feature-tier-spec-creation.md`
  summary: The frontmatter `id` string (`` `feat-${createdAt}-${name}` `` / `` `patch-${createdAt}-${name}` ``) is constructed independently in both `new-spec.ts` (the caller) and each tier's template module (`templates/feature.ts`, `templates/patch.ts`), rather than computed once and passed in — two format strings per tier that could silently drift apart if one is ever changed without the other.
  evidence: Confirmed by reading `createFeatureSpec`/`createPatchSpec` alongside `renderFeatureSpec`/`renderPatchSpec` — both the caller and the template independently interpolate the same `feat-<date>-<name>`/`patch-<date>-<name>` pattern. Pre-existing for patch tier since Story 1.2 (not previously logged) and reproduced for feature tier in Story 1.3; a real minor duplication risk, but consolidating it touches multiple already-tested call sites, so it's a deliberate refactor for a future pass rather than a one-line fix now.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-feature-tier-spec-creation.md`
  summary: `waypoint new-feature`'s `--help` description mentions "one approval gate," but no `waypoint approve` command exists yet — not even as a discoverability stub the way `new-system` has one — so a user reading `--help` has no way to see that the referenced approval workflow is intentionally deferred to Epic 3.
  evidence: Confirmed by reading `program.ts`'s `new-feature` registration and the `STUB_COMMANDS` list — `approve` isn't registered in either form. Whether to add an `approve` stub now or reword the help text is a product/UX decision, not a mechanical fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-feature-tier-spec-creation.md`
  summary: `createFeatureSpec`'s two collision checks (spec path across all three tiers, and the ledger path) use the same case-sensitive `existsSync` comparisons as `createPatchSpec`, so the case-insensitive-filesystem gap already logged above for patch tier now spans two files (spec + ledger) instead of one — a case-only name clash (e.g. `Demo-Feature` vs. an existing `demo-feature`) could produce a mismatched spec/ledger pair on macOS or Windows rather than just one wrong file.
  evidence: Confirmed by reading `createFeatureSpec`'s `SPEC_TIERS` loop and its ledger-path check — neither does any case-folding. Same underlying gap as the existing patch-tier entry above; noted separately here because the failure mode is now two files instead of one, and because it deserves its own naming-policy decision alongside patch tier's, not a silent extension of the old entry.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-cross-platform-and-vendor-neutrality-verification.md`
  summary: `.github/workflows/ci.yml` triggers its full 3-OS matrix on every `push` to every branch with no path or branch filtering, so a doc-only or WIP commit on a feature branch costs the same as a real code change.
  evidence: Confirmed by reading the workflow's `on:` block — plain `push:`/`pull_request:` with no `paths`/`paths-ignore`/`branches` filters. Whether (and how) to filter is a deliberate cost/coverage tradeoff, not a mechanical fix — e.g. excluding `docs/**`/`*.md` risks silently skipping a doc change that also touched code in the same commit if the filter is too broad.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-cross-platform-and-vendor-neutrality-verification.md`
  summary: The new CI workflow exists but isn't wired into GitHub's branch-protection "required status checks," so a red 3-OS matrix run doesn't yet block a PR from being merged — the epic's stated goal ("proven, not left as an assumed property") is only half-delivered until this is turned on.
  evidence: Confirmed no code in this repo configures required status checks (that lives in GitHub's branch-protection settings, not a repo file); checking via `gh api repos/.../branches/develop/protection` returned a 403 — this private repo's current GitHub plan doesn't support branch protection at all, so this can't even be turned on today without a plan upgrade or making the repo public. Either way it's a repo-settings/billing decision affecting shared merge policy, not a code change — a deliberate, explicit action for the human to take (or ask for) rather than something a code review's patch batch should apply.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-system-tier-spec-scaffolding.md`
  summary: `waypoint new-system`'s `--help` description mentions "phased approval," but no `waypoint approve` command or phase-gating mechanism exists yet — same class of gap already logged for `new-feature`'s "one approval gate" text, now also true for `new-system`.
  evidence: Confirmed by reading `program.ts`'s `new-system` registration and the full `STUB_COMMANDS`/command list — no `approve` command or stub exists anywhere. Whether to add an `approve` stub now or reword both commands' help text is a product/UX decision spanning both tiers, not a mechanical fix to this story alone.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-system-tier-spec-scaffolding.md`
  summary: `createSystemSpec`'s directory-based collision check (`specs/systems/<name>`) is case-sensitive like `createPatchSpec`/`createFeatureSpec`'s file-based checks, so the same case-insensitive-filesystem gap already logged for patch/feature tier now also applies to system tier's directory collision.
  evidence: Confirmed by reading `specTierCollisionPath`'s `systems` branch — plain `existsSync`, no case-folding. Same underlying gap as the existing patch/feature entries above, extended here to the new directory-shaped check; still needs one deliberate naming-policy decision covering all three tiers, not a per-tier mechanical fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-system-tier-spec-scaffolding.md`
  summary: `createPatchSpec`/`createFeatureSpec`/`createSystemSpec` still each inline their own copy of the validate-name → install-check → cross-tier-collision-loop sequence; only the per-tier path computation (`specTierCollisionPath`) was factored into a shared helper, not the sequence itself.
  evidence: Confirmed by reading all three functions — the same four-line block (`isValidName` check, `isInstalled` check, `for (const tier of SPEC_TIERS)` loop) is repeated verbatim in each. A real consolidation opportunity (e.g. a shared `assertCreatable(cwd, name)` prelude), but deciding its exact shape is a deliberate refactor for a future pass, not a one-line fix now — especially since all three functions are independently well-tested today.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-update-spec-via-delta.md`
  summary: `updateSpec`'s ledger write and spec-file write are two independent, non-atomic `writeFile` calls — if the second write fails after the first succeeds (disk full, permissions, process killed), the two artifacts end up transiently out of sync (e.g. the ledger gains new rows but the spec's delta heading never gets appended, or vice versa).
  evidence: Confirmed by reading `updateSpec` directly — no rollback or transactional wrapping around either write. Lower urgency than Epic 1's creation-time write gaps: `update`'s own sync logic re-scans the spec's current `ADDED` content from scratch on every run (idempotent by description-matching), so a subsequent retry naturally catches up rather than leaving a permanently broken, unrepairable state — but it's still a real gap worth a deliberate fix (e.g. write both to temp files then rename) rather than a quick patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-check-drift-detects-stale-specs.md`
  summary: `check-drift`'s path-existence check (`existsSync`) is case-sensitive, so on the common case-insensitive dev filesystems (macOS APFS, Windows) a reference like `` `Foo.ts` `` against an actual `foo.ts` reports as resolved — exactly the class of drift (breaks on case-sensitive Linux CI) the tool exists to catch, systematically missed on the most common local dev machines.
  evidence: Confirmed by reading `resolvePathReference` — a plain `existsSync(path.join(cwd, pathValue))` with no case normalization or comparison against the actual directory listing. Same underlying class of gap already logged above for `new-patch`/`new-feature`/`new-system`'s name validation, now also affecting `check-drift`'s existence checks — still needs one deliberate cross-cutting decision, not a per-command fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-check-drift-detects-stale-specs.md`
  summary: `collectRepoFiles`'s recursive walk only branches on `entry.isDirectory()`/`isFile()`, so a symlinked file or directory is silently skipped entirely — a symbol reference resolving only through a symlinked path would be wrongly reported as stale even though its real target exists.
  evidence: Confirmed by reading the walk function directly — no `isSymbolicLink()` handling at all. Same class of gap already logged for `scaffold.ts`'s `checkConflict` (Story 1.1), which also silently follows/ignores symlink semantics without a deliberate policy; worth one shared decision covering both call sites rather than a one-off fix here.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-check-drift-detects-stale-specs.md`
  summary: `EXCLUDED_DIR_NAMES` covers only `.git`/`node_modules`/`dist`/`.waypoint` (per the frozen spec's own list) — it doesn't exclude other common build/output directories (`build`, `coverage`, `.next`, `vendor`, etc.) and doesn't consult `.gitignore`, so the repo-wide symbol search can be slower than necessary and could pull generated content into a match, masking real drift behind a leftover build artifact.
  evidence: Confirmed by reading `EXCLUDED_DIR_NAMES` — exactly the four directories the spec names, nothing more. Deciding whether to hardcode a broader list or consult `.gitignore` properly is a deliberate scope decision (and a bigger feature in the `.gitignore` case), not a one-line fix to this story's frozen exclusion list.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-tier-classification-via-config-driven-globs.md`
  summary: `classifyChangedFiles`'s per-file frontmatter-override reads (`existsSync` + `readFile`) run sequentially in a `for` loop rather than concurrently, which will scale linearly with batch size once Story 3.2's pre-commit gate drives this at up to 2,000 changed files under a sub-2-second budget.
  evidence: Read `packages/core/src/gate-classify.ts`'s `classifyChangedFiles` directly — the `for (const filePath of filePaths)` loop awaits `frontmatterOverrideTier` one path at a time instead of batching with `Promise.all`; not a correctness bug and out of this story's scope (no CLI, no perf requirement here), but the epic-3 perf budget belongs to whichever story actually wires this into the gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-tier-classification-via-config-driven-globs.md`
  summary: `classifyChangedFiles` has no documented or tested caller contract for how a file rename's vacated old path should be represented in the input batch (a separate deletion entry vs. simply omitted), and test coverage for the frontmatter-override step doesn't reach `architecture.md`/`adr.md` (which carry no frontmatter by this codebase's own convention) or the "override resolves to the same tier the glob already produced" case where `reason` still flips to `'frontmatter-override'` with no tier change.
  evidence: Confirmed by reading `gate-classify.ts` and `gate-classify.test.ts` directly — none of these represent a known defect in the current behavior, but Story 3.2 is the first real caller of this primitive and will need to settle the rename-input contract explicitly, and the untested paths are worth pinning down before more logic builds on top of them.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-pre-commit-gate-blocks-missing-spec-deltas.md`
  summary: The `waypoint gate` CLI command, the `git diff --cached` shell-out, `waypoint install`'s new hook-file writing (`.git/hooks/pre-commit` and `.git/hooks/pre-merge-commit`, executable, marker-tagged, preserve-foreign-hook semantics), and an end-to-end real-git-repo test proving a commit is actually rejected are deferred to a follow-up spec.
  evidence: The full Story 3.2 spec (core `gate()` function + CLI wiring + hook installation) estimated ~3600 tokens, more than double the 1600-token risk threshold. Split along the same "CLI-less primitive vs. consuming mechanism" seam Story 3.1/3.2 already established: this spec now covers only the pure `gate()` function and its unit tests; the follow-up spec consumes it exactly as Story 3.2 was always going to consume Story 3.1's `classifyChangedFiles`. All the empirical git-hook-behavior findings that inform the follow-up spec (pre-merge-commit vs. pre-commit, the amend-detection limitation, the hand-rolled-vs-Husky decision) are already durably recorded in `docs/architecture.md` v0.7, so nothing is lost by deferring the wiring itself.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-pre-commit-gate-blocks-missing-spec-deltas.md`
  summary: `gate()` doesn't deduplicate repeated paths within a single `changedFiles` batch — the same enforced-tier path appearing twice would produce two separate (but not incorrect) violation entries for the same file.
  evidence: Confirmed by reading `packages/core/src/gate.ts`'s `classifications.filter(...).map(...)` — no dedup step. Low real-world likelihood since the only realistic caller (`git diff --cached --name-only`, wired in a follow-up spec) never emits duplicate paths, so this isn't worth guarding against now, but worth a note before any other caller is added.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-pre-commit-gate-blocks-missing-spec-deltas.md`
  summary: `packages/core/src/gate-classify.ts`'s `frontmatterOverrideTier` (Story 3.1, already shipped) checks `isSpecTierPath(normalizeSlashes(filePath))` but then builds its `existsSync` check via `path.join(repoRoot, filePath)` using the raw, unnormalized `filePath` — the same inconsistency Story 3.2's review caught and fixed in `gate.ts`'s own new `existsSync` check.
  evidence: Confirmed by reading `frontmatterOverrideTier` directly and by reproducing the failure mode in `gate.ts` before fixing it there (a backslash-separated path on POSIX fails an unnormalized `path.join`+`existsSync` even when `isSpecTierPath` correctly recognizes it after normalization). Very low real-world likelihood since every current caller produces forward-slash paths, but it's the same latent gap, now fixed in the newer file but not the older one; worth a small follow-up fix to `gate-classify.ts` for consistency.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-gate-cli-and-hook-installation.md`
  summary: `scaffold()` never detects a repo already using `core.hooksPath` to redirect hooks elsewhere (e.g. Husky's common setup) before writing to `.git/hooks/*` — in that configuration, `waypoint install` reports success while git never actually invokes the hooks it just wrote, so enforcement silently never activates.
  evidence: Confirmed by reading `scaffold.ts`'s hook-installation logic directly — no `core.hooksPath` check exists anywhere. Detecting it correctly without violating this codebase's existing "install never shells out to git" guarantee (enforced by `vendor-neutrality.test.ts`) would require parsing `.git/config` directly (INI-style, possibly with `include.path` directives) rather than shelling to `git config --get`, which is more design work than a quick patch; real but affects only repos with a pre-existing custom hooksPath, not the default case.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-gate-cli-and-hook-installation.md`
  summary: The installed hook scripts (`exec npx waypoint gate`) depend on `npx`/`node` being resolvable on `PATH` at the moment git invokes the hook — a common real-world failure mode for Node-based git hooks (some git GUI clients and non-interactive shells run hooks with a minimal `PATH` that excludes user-installed Node tooling, e.g. nvm-managed installs not sourced into non-login shells).
  evidence: Inherent to the hand-rolled-hook-script design chosen in Story 3.2 part 1's planning (see docs/architecture.md v0.7) specifically to avoid a Husky dependency — Husky faces the identical limitation, so this isn't a regression introduced by this story, just an accepted, real limitation of any Node-based git hook approach. Worth a documented workaround (e.g. hardcoding a resolved node path at install time) if it becomes a real adoption blocker.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-gate-cli-and-hook-installation.md`
  summary: `HOOK_MARKER` idempotency detection is a plain `.includes()` string check on file content — fragile to incidental re-encoding (BOM insertion, line-ending conversion by an editor) misclassifying Waypoint's own previously-installed hook as foreign on a future `waypoint install`. Relatedly, there's no versioning scheme, so a future story that needs to change `HOOK_SCRIPT_CONTENT` has no way to distinguish "old Waypoint hook, safe to upgrade" from "foreign hook, never touch."
  evidence: Confirmed by reading the marker-check logic in `scaffold.ts` directly. Low likelihood in practice (nothing in Waypoint's own flow re-encodes the hook file after writing it), but worth a versioned marker (e.g. `# Installed by waypoint install (v1) — do not edit directly.`) if hook content ever needs to change.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-gate-cli-and-hook-installation.md`
  summary: `installCommand` prints `scaffold()`'s new `warnings` entries via `console.log` (stdout), while `gateCommand`'s own failure/violation output uses `console.error` (stderr) — an inconsistent convention across the two commands for "something's not fully working" output.
  evidence: A stylistic/UX judgment call with no single clearly-correct answer (installs still succeed overall even with a warning, arguably belonging on stdout; but a warning about degraded enforcement arguably deserves stderr's attention-grabbing convention) — not a functional bug either way, so left as a defer rather than a unilateral pick.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-task-ledger-with-verify-only-completion.md`
  summary: `verify.ts`'s ledger and `.gate-state` writes are not atomic (no temp-file+rename) — a crash exactly mid-`writeFile` could leave either file truncated, and for `.gate-state` specifically (a single JSON blob shared by every task in a spec) a torn write could threaten the "multiple tasks' hashes preserved" guarantee.
  evidence: Confirmed by reading `verify.ts` directly. Not treated as this story's own gap: this codebase has no established atomic-write convention anywhere (`scaffold.ts`, `gitignore.ts`, `new-spec.ts` all write directly), so introducing one here would be new scope beyond matching existing risk tolerance, not a fix to a story-specific oversight.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-task-ledger-with-verify-only-completion.md`
  summary: `verifyTask` doesn't detect a ledger with two task rows sharing the same `id` — only the first match from `tasks.find()` is ever read or written, so a hand-edited duplicate id would silently ignore the intended second row.
  evidence: Confirmed by reading the `tasks.find((t) => t.id === taskId)` call sites directly. Very low real-world likelihood (ledgers are machine-generated with sequential `t1`/`t2`/`t3` ids; a collision requires a deliberate or careless hand-edit), not worth a dedicated guard for MVP.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-task-ledger-with-verify-only-completion.md`
  summary: `verifyTask`'s `commit-failed` outcome doesn't distinguish a `writeFile` failure (the ledger write itself) from an actual `git add`/`git commit` failure — both produce the same message ("could not commit the ledger update"), which could misdirect troubleshooting for the rarer writeFile-failure case.
  evidence: Confirmed by reading the single try/catch wrapping all three operations in `verify.ts`. Low value fix relative to cost — the message is still broadly accurate (something in the write-then-commit sequence failed and was rolled back) even when imprecise about which step.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-human-only-approval.md`
  summary: `approve.ts`'s `parseFrontmatterObject` silently swallows any YAML parse error on a spec's frontmatter block and returns `{}` rather than surfacing a clear error, so a spec whose frontmatter is malformed in a way that still keeps the fixed-shape regexes (`status: draft`, etc.) matchable would have its write proceed against a `{}` read-model — e.g. `fm.status === 'approved'`/`readPhaseApprovals(fm)` would both silently treat real, unparsed data as absent.
  evidence: Confirmed by reading `parseFrontmatterObject` directly (three review lenses independently flagged this) — the `catch { return {}; }` has no distinguishing signal for "genuinely empty" vs. "failed to parse." Requires a specifically-malformed-YAML input that nonetheless keeps every fixed-shape line intact, an unusual and likely hand-crafted/adversarial shape; deferred rather than patched immediately, since surfacing this properly means deciding whether a parse failure should hard-fail the whole `approve` call (a behavior change) rather than a one-line fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-human-only-approval.md`
  summary: `readPhaseApprovals` silently drops any `phase_approvals` array entry that isn't a well-formed `{phase: number, ...}` object, so a hand-corrupted entry is not just ignored on read but permanently lost the next time `approve` rewrites the line (the array is re-rendered from only the entries that survived parsing).
  evidence: Confirmed by reading `readPhaseApprovals` directly (flagged by two review lenses). Requires a hand-edited `phase_approvals` array to trigger; deferred as an adversarial-input-only scenario, not a gap in the story's own normal-usage behavior.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-human-only-approval.md`
  summary: `readLedgerDistinctPhases` accepts any ledger task whose `phase` field is `typeof 'number'`, which includes a hand-typed YAML `.nan` (parsed as JavaScript `NaN`) — `NaN` would pass the numeric check, enter the `Set`, and then break the ascending sort comparator (`NaN` compares unequal to everything, including itself).
  evidence: Identified by the edge-case-hunter review lens; requires a hand-typed `.nan` literal in the ledger YAML, an adversarial/corrupted-input scenario rather than anything reachable through this codebase's own ledger-writing paths (`verify.ts`, `update-spec.ts`'s sync pass). Deferred rather than patched now.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-human-only-approval.md`
  summary: There is a narrow TOCTOU window between `findSpecById`'s read of a spec file and `approveFeatureSpec`/`approveSystemSpec`'s own separate `readFile` of the same path — if the file is deleted or replaced in between, a raw `ENOENT` (or similarly unwrapped filesystem error) could escape instead of a clear, `approve`-specific error.
  evidence: Identified by the edge-case-hunter review lens. Matches an already-accepted class of narrow race in this codebase elsewhere (e.g. `update-spec.ts`'s own re-read-after-`findSpecById` pattern) that has never been specially guarded; deferred for consistency with that precedent rather than treated as a Story-3.4-specific gap.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-human-only-approval.md`
  summary: A hand-edited `phase_approvals` entry missing `approved_at` entirely would have `record.approved_at` read as `undefined`, and `readPhaseApprovals`'s fallback (`String(record.approved_at)`) persists that as the literal string `"undefined"` on the next rewrite, rather than treating the entry as malformed.
  evidence: Identified by the edge-case-hunter review lens. Requires a hand-corrupted `phase_approvals` entry; same adversarial-input-only bucket as the other `readPhaseApprovals`/`parseFrontmatterObject` findings above, deferred together with them rather than patched individually.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-human-only-approval.md`
  summary: `LedgerNotFoundError` is reused for three distinct failure shapes in `readLedgerDistinctPhases` (the ledger file is missing, the ledger YAML fails to parse, and the ledger parses but lacks a `tasks` array) — all three surface as the same error class with only the message text distinguishing them, which could complicate future error-specific handling.
  evidence: Identified by the blind-hunter review lens; a naming/taxonomy precision nit rather than a behavioral gap — a caller catching `LedgerNotFoundError` today gets a correct, clear message in all three cases. Deferred as a cosmetic follow-up, not worth a new error class for three low-likelihood failure shapes.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-ci-enforces-the-same-gate-plus-done-claim-correctness.md`
  summary: `checkDoneClaims`'s done-claim check is a full, repo-wide sweep of every `tasks/**/*.ledger.yaml` file on every `--ci` run (required by epics.md's own Story 3.5 AC, not an implementer choice), which is fundamentally incompatible with sustained squash-merge use: once any repo's first PR is squash-merged, that PR's `linked_commit` is rewritten out of the target branch's history, and every *subsequent* PR's `--ci` run re-checks that same old task's ancestry against the new (squashed) `HEAD`, where the original `linked_commit` no longer resolves — producing a permanent, unrelated build failure with no recovery path short of hand-editing that ledger row.
  evidence: Identified by the adversarial review lens; confirmed by re-reading `docs/architecture.md`'s existing v0.5 "squash-merge is a non-issue" reasoning, which only addresses the task's *own* PR being checked pre-merge, not this cross-PR interaction with the full-repo sweep introduced by Story 3.5. User-confirmed decision: document as a known limitation for now (see `docs/architecture.md` v0.8's Change Log entry and the accompanying Core Workflows caveat) rather than fix in Story 3.5 — a real fix means either scoping the check to diff-touched ledgers only (weakens the cross-PR guarantee, requires amending epics.md's AC) or a different mechanism entirely (e.g. re-verifying against the PR branch's own recorded tip rather than the target branch's current `HEAD`). Revisit if/when a consuming repo actually hits this in practice, or as a deliberate follow-up story.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-ci-enforces-the-same-gate-plus-done-claim-correctness.md`
  summary: `done-claim.ts`'s `collectLedgerFiles` walk checks `entry.isDirectory()`/`entry.isFile()` on each `Dirent`, both of which are `false` for a symlink — so a `*.ledger.yaml` delivered as a symlink, or a symlinked subdirectory under `tasks/`, is silently skipped and never checked at all.
  evidence: Identified by the adversarial review lens; confirmed by reading `collectLedgerFiles` directly (no `entry.isSymbolicLink()` handling anywhere). Same underlying class of gap already logged twice before in this file for `check-drift.ts`'s `collectRepoFiles` (Story 2.2) and `scaffold.ts`'s `checkConflict` (Story 1.1) — deferred here for consistency with that established precedent ("worth one shared decision covering both call sites rather than a one-off fix here," now extended to a third). Also judged low-incremental-risk on its own terms: a fabricator willing to go to the length of committing a symlink to hide a done-claim from this specific check could just as easily avoid the `*.ledger.yaml` naming pattern entirely, which nothing else in the system validates against a spec's expected task list — so this isn't meaningfully easier than bypasses already possible without any symlink trick.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-ci-enforces-the-same-gate-plus-done-claim-correctness.md`
  summary: `checkDoneClaims` runs one blocking, sequential `execFileSync` git shell-out per `done` task with no parallelism or short-circuit (e.g. for a task whose hash is already locally verifiable), so wall-clock cost scales linearly with the total number of historical `done` tasks across the whole repo, unbounded.
  evidence: Identified by the adversarial review lens. Not treated as a Story 3.5 gap: the story's own AC only requires completing within a 60-second CI budget "against a repo with up to 2,000 tracked files and 50 open specs," and the automated perf test at exactly that scale (250 done tasks across 50 ledgers, combined with `gate()`'s own full-diff check) completes in ~4.5 seconds — better than 13x margin under budget. Parallelizing would add real complexity (bounded-concurrency pooling around `execFileSync`) for a scale well beyond what's currently required; worth revisiting if a real consuming repo's historical done-task count ever approaches a scale that threatens the budget in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-agents-md-generation.md`
  summary: `renderAgentsMd()`'s Available Commands section is a hand-maintained static list, since `packages/core` has no dependency on `packages/cli` and can't introspect `program.ts`'s registered commands at runtime — a future command added, renamed, or removed in `program.ts` without a matching manual update to `agents-md.ts` would drift out of sync with what's actually installed.
  evidence: A known, accepted tradeoff documented directly in `agents-md.ts`'s own doc comment (not discovered by review — flagged proactively during implementation). Matches this codebase's existing tolerance for a small number of independently-maintained duplicate-content spots (e.g. the `feat-`/`patch-` id-construction pattern already logged above for Story 1.3). The underlying hand-maintained-list tradeoff is still accepted (not worth a cross-package dependency inversion or a build-time codegen step for MVP's command surface size), but the review round's own regression test (see this spec's Change Log) now at least makes a forgotten manual update fail the test suite loudly, instead of drifting silently.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-agents-md-generation.md`
  summary: `renderAgentsMd()`'s Role Prompts section tells an agent to "point any agent at one of these files... depending on which role that agent is playing," but as of this story shipping, all four `roles/*.md` files are still Story 1.1's placeholder content (`<!-- ... role prompt — generated by waypoint install; content populated by Epic 4. -->`) — Story 4.2 (the very next story in this epic) is what actually fills them in.
  evidence: Identified by the adversarial review lens. A real but short-lived gap: any `waypoint install` run between Story 4.1 shipping and Story 4.2 shipping produces an `AGENTS.md` that describes placeholder files as if they already carry real role guidance. Not patched now since it self-resolves the moment Story 4.2 ships (no installed base exists yet to be affected in the meantime), and hedging the wording now would just mean un-hedging it again in the very next story.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-agents-md-generation.md`
  summary: A repo that ran `waypoint install` before this story shipped keeps its old one-line `AGENTS.md` placeholder forever — `scaffold()`'s existing "already exists → preserve, never overwrite" rule (Story 1.1) treats the placeholder identically to a genuinely user-customized file, with no way to distinguish "never touched, safe to upgrade" from "hand-edited, must preserve."
  evidence: Identified by the adversarial review lens; confirmed via `scaffold.test.ts`'s own existing reinstall-preservation test, which already encodes this as the intended behavior for every scaffolded path. Not fixed in Story 4.1: doing so would mean adding new placeholder-detection logic to `scaffold()`, which this story's own frozen Boundaries explicitly ruled out ("this story adds no new preserve/detection code") — the underlying idempotency design is Story 1.1's, not this story's to change. Low real-world urgency today since Waypoint has no installed base yet (pre-1.0); worth a deliberate follow-up (e.g. detecting the exact placeholder string specifically, distinct from any other content) if/when real installs predate a future content-generating story like this one.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-agents-md-generation.md`
  summary: `docs/architecture.md`'s packaged-templates source-tree sketch is broadly stale beyond the one `agents-md.template` line this story's review fixed — it also still shows `feature.md` and a `system/{prd.md,architecture.md,adr.md}` subdirectory as separate packaged template files, when the actual implementation (Story 1.2/1.3) is a single embedded-TypeScript-string function per tier (`patch.ts`/`feature.ts`/`system.ts`), generating all of a System spec's file content at scaffold time in the consuming repo rather than from packaged template files.
  evidence: Identified while independently verifying the adversarial review lens's narrower `agents-md.template` finding — reading the surrounding tree block directly showed the staleness predates this story, spanning Epic 1's own templates. Out of Story 4.1's scope to fully rewrite (a broader documentation-hygiene pass touching stories this one doesn't own); the one line directly relevant to this story's own deliverable (`agents-md.template`) was fixed, the rest deferred.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-role-prompts.md`
  summary: A repo that ran `waypoint install` before this story shipped keeps its old placeholder `roles/*.md` content (`<!-- ... role prompt — generated by waypoint install; content populated by Epic 4. -->`) forever — `scaffold()`'s existing "already exists → preserve, never overwrite" rule (Story 1.1) treats the placeholder identically to a genuinely user-customized file, with no way to distinguish "never touched, safe to upgrade" from "hand-edited, must preserve."
  evidence: Identified by the edge-case-hunter review lens — the exact same class of gap already logged for Story 4.1's `AGENTS.md` (same underlying preserve-on-reinstall design, same fix constraint: this story's frozen Boundaries explicitly ruled out adding new preserve/placeholder-detection logic to `scaffold()`). Not fixed here for the same reasons as that entry: low real-world urgency given no installed base exists yet (pre-1.0), and the underlying idempotency design belongs to Story 1.1, not this story. Worth a single deliberate follow-up covering both `AGENTS.md` and all 4 role files together, if/when real installs predate a future content-generating story like this one.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-waypoint-status.md`
  summary: `findAllSpecs` (new, added for this story) never detects two spec files sharing the same frontmatter `id`, unlike `findSpecById`'s existing `DuplicateSpecIdError` — `computeStatus` would then emit two separate, visually-identical `waypoint status` entries for the same id, with no signal that anything is wrong, and would double-count one logical spec toward its tier totals.
  evidence: Identified independently by two review lenses (adversarial and edge-case-hunter). Matches this codebase's own already-accepted precedent from Story 3.3's deferred-work entry for the identical underlying concern (a ledger with two task rows sharing the same id) — "very low real-world likelihood... not worth a dedicated guard for MVP." Requires a deliberate hand-edit or a rare merge-conflict-style collision to trigger; `findAllSpecs`'s own doc comment already explains this was a deliberate design choice (enumeration, not uniqueness validation) rather than an oversight. Worth a shared fix (e.g. one collision-detection helper reused by both `findSpecById` and `findAllSpecs`) if this ever surfaces in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-waypoint-status.md`
  summary: `computeStatus` re-reads and re-parses the same `.waypoint/.gate-state/<spec-id>.json` file once per `done`-claiming task instead of once per spec, and performs every spec's/task's reads sequentially with no batching (`Promise.all`) — both add up to more filesystem/parse work than strictly necessary as a repo's spec/task count grows.
  evidence: Identified by the adversarial review lens. Not treated as a Story 5.1 gap: this story carries no AC-mandated performance budget (unlike, e.g., Story 3.5's explicit 60-second/2,000-file/50-ledger requirement), and the actual cost — re-reading one small JSON file a handful of times per spec — is negligible at the scale this codebase's own NFR4 already establishes as its MVP target. Revisit only if a real consuming repo's spec/task count ever grows large enough to make this measurably slow in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-waypoint-status.md`
  summary: `SpecStatusEntry`'s `approved: boolean | 'not-applicable'` and `tasks: TaskStatus[] | 'not-applicable' | 'ledger-error'` are independent fields rather than a true discriminated union keyed on tier, so the type system permits (but `computeStatus` never actually produces) an inconsistent combination like `approved: 'not-applicable'` paired with a real `tasks` array.
  evidence: Identified by the adversarial review lens as a type-safety improvement, not a demonstrated bug — confirmed by reading `computeStatus` directly that it never produces an inconsistent pairing in practice. A real discriminated-union refactor would also touch the CLI's rendering logic for limited benefit; deferred as a speculative type-purity improvement rather than a fix to an observed defect, especially since this is the final story of the MVP roadmap with no future consumer that would benefit from the generalization.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-waypoint-status.md`
  summary: `waypoint status`'s closing criterion trusts a spec's raw frontmatter `status: approved` field at face value, with no cross-check against `approved_by`/`approved_at` being non-null (Feature tier) or `phase_approvals` actually covering every distinct ledger phase (System tier) — the adversarial review lens proposed this as an analogue to the `CORRUPTED`-task hash check, but for the approval half of the closing criterion.
  evidence: Rejected, not deferred: cross-checking `approved_by`/`approved_at`/`phase_approvals` would add zero real defense, since all three live in the exact same hand-editable spec-file YAML as `status` itself, with no cryptographic backing analogous to `.gate-state`'s hash for task completion — a hand-editor motivated enough to fake `status: approved` can just as easily fake the accompanying fields in the same edit. Matches the identical reasoning already applied this session to reject a parallel finding on Story 3.5's `checkDoneClaims` (checking `verified_by_gate` there was rejected for the same underlying reason). FR8's human-only-approval guarantee has always been explicitly a documentation-layer convention rather than a technically-enforced one, per `approve.ts`'s own module doc comment and `docs/prd.md`'s own Background Context.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: `readStoredHash` (reading a task's stored `.gate-state` hash) is defined byte-for-byte identically in both `packages/core/src/verify.ts` and `packages/core/src/status.ts` — a small, pure, 10-line helper duplicated rather than shared, unlike `computeLedgerTaskHash` (already exported from `verify.ts` specifically for this kind of cross-module reuse).
  evidence: Identified during the whole-MVP retrospective's duplication-map sweep. Distinct from this codebase's deliberate "self-contained reader per module" convention (each module's surrounding ledger-parsing logic solves a genuinely different problem and earns its separateness) — this one specific helper doesn't. Zero current risk (both copies are correct and current); deferred rather than fixed now because consolidating means reopening two already-shipped, well-tested modules for a small win. Worth doing the next time either file is touched for an unrelated reason.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: `packages/core/src/update-spec.ts` (634 lines) now holds three genuinely distinct concerns: generic spec-discovery primitives now reused by other commands (`FoundSpec`/`findSpecById`/`findAllSpecs`, added across Stories 2.1 and 5.1), shared byte-fidelity utilities other modules import (`splitFrontmatter`/`todayIsoDate`, reused by `approve.ts`), and its own original delta-sync logic (`updateSpec` and its dozen or so supporting functions) — a reader hunting for "how do I discover a spec by id" has to open a file named for an unrelated command to find it.
  evidence: Identified during the whole-MVP retrospective's god-class/duplication sweep. Not yet a god-class — everything in the file is well-tested and independently correct, just organizationally mismatched to its own filename. Grew because two later, unrelated stories each found it natural to extend this file rather than extract a `spec-discovery.ts`. Deferred: no user-facing benefit to splitting now; a legitimate candidate if a fourth unrelated concern ever lands here.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: Four independent, never-unified implementations of "is this id path-safe" exist across the codebase, in two different styles: `update-spec.ts`/`approve.ts` share an identical `SPEC_ID_SHAPE_PATTERN` regex (a strict allowlist, defined byte-for-byte in each file separately); `verify.ts`/`status.ts` each independently define a simpler character-denylist (`isPathUnsafeId`/`validatePathSafeIds`) — `status.ts`'s own doc comment explicitly cites `verify.ts`'s as precedent without reusing it. `verify.ts`'s use of the weaker style is arguably correctly calibrated (CLI-argument-sourced ids, a different trust boundary than frontmatter content), but `status.ts`'s id is *also* frontmatter-sourced — the same adversarial-input risk `approve.ts`/`update-spec.ts` already decided warrants the stricter check — so `status.ts` arguably has the weaker guard on the wrong side of the trust boundary.
  evidence: Converged independently across three separate investigations run during the whole-MVP retrospective (a dedicated path-safety audit, a duplication-map sweep, and two `bmad-review` lenses), which is itself the signal this is real rather than a one-off nitpick. No unpatched security gap results today — every guard, weak or strict, already blocks the traversal characters that matter. Deferred: extract one shared helper, or at minimum upgrade `status.ts` to the stricter check, the next time any of these four files is touched for an unrelated reason.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: A lost-update race exists on spec-*file* writes (as opposed to the ledger) between `approve.ts` and `update-spec.ts`: both read the spec `.md` file's raw text at call start and write a modified version derived from that snapshot, with no lock between them, unlike the ledger (which at least `verify.ts` locks).
  evidence: Identified during the whole-MVP retrospective's cross-story boundary review. Lower urgency than the analogous ledger-locking gap (spec files are edited far less frequently/concurrently than the ledger, which every `verify` call touches) — deferred as the same underlying gap at lower priority; worth closing alongside a future fix to the ledger-locking finding if that work is ever generalized.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: `approve.ts`'s `readLedgerDistinctPhases` doc comment inaccurately claims to mirror `verify.ts`'s non-throwing `readLedgerFile` error-handling pattern, but the function actually throws `LedgerNotFoundError` on the identical missing/malformed-ledger condition.
  evidence: Identified during the whole-MVP retrospective's cross-story boundary review. Not a behavioral bug — the CLI wrapper already catches `LedgerNotFoundError` in its known-error list and reports it cleanly, so the failure mode is a clean, handled error, not a raw crash. Only the doc comment's claim about matching precedent is wrong. Deferred as a comment-accuracy fix, not a behavior change.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: `done-claim.ts`'s `COMMIT_HASH_PATTERN` is documented as accepting "a full 40-character hex SHA" but the actual regex (`/^[0-9a-f]{4,40}$/i`) accepts 4-40 characters (looser than documented), and is incompatible with a SHA-256 git repository, where a legitimate `git rev-parse HEAD` returns a 64-character hex string.
  evidence: Identified during the whole-MVP retrospective's cross-story boundary review. SHA-1 remains git's near-universal default, so this is a latent, low-urgency gap rather than a live bug; this codebase's own Technical Assumptions (`docs/prd.md`) already name git as the sole supported VCS with no SHA-256 commitment either way. Deferred: tighten the regex to match the documented 40-character claim (or make both the writer and validator explicitly hash-algorithm-agnostic) the next time this file is touched.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: `done-claim.ts`'s ledger walker (`collectLedgerFiles`) explicitly supports a nested `tasks/` layout (e.g. `tasks/nested/feat-demo.ledger.yaml`), but no other ledger consumer (`update-spec.ts`, `approve.ts`, `status.ts`) or any real scaffolding code ever produces or expects one.
  evidence: Identified during the whole-MVP retrospective's cross-story boundary review. Speculative generality with no current producer — a ledger relocated into a subdirectory would be found and validated by `waypoint gate --ci` but reported as unreadable (`[LEDGER ERROR]`/`LedgerNotFoundError`) by every other command. Deferred: reconcile the convention (restrict to flat, or teach the other three consumers the same walk) the next time any of these four files changes.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: Two minor, low-priority doc-comment inaccuracies with no behavioral consequence: `verify.ts`'s `validatePathSafeIds` doc comment claims `taskId` is used in path construction when in the actual code only `specId` is; `status.ts` folds a path-unsafe-id spec into the generic `[LEDGER ERROR]` state, losing the specific diagnostic `verify.ts` gives for the identical condition on its own CLI-argument path.
  evidence: Identified during the whole-MVP retrospective's cross-story boundary review. Cosmetic precision issues, not correctness gaps — deferred as low-value doc/UX polish for whenever either file is next touched.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: `docs/architecture.md`'s existing deferred packaged-templates-source-tree-sketch staleness entry (originally scoped to just the `agents-md.template` → `agents-md.ts` line) is narrower than the sketch's actual staleness — it also still shows `core/drift/`, `core/ledger/`, `core/delta/` as subdirectories and `scripts/gate.sh` as a file, none of which exist; the real implementation is flat files directly under `packages/core/src/`, with no `scripts/gate.sh` anywhere.
  evidence: Identified during the whole-MVP retrospective's spec-to-implementation reconciliation sweep. No new deferred entry needed — this widens the scope note on the existing entry logged under Story 4.1's spec, so a future documentation-hygiene pass fixes the whole sketch in one pass rather than two.

- source_spec: `_bmad-output/implementation-artifacts/epic-1-5-mvp-retro-2026-08-25.md`
  summary: Fixing the fresh-install bootstrap-blocking bug (Finding 1 — a fresh `waypoint install`'s own first commit was blocked by the gate it just installed) required patch-classifying `.gitignore`, `.waypoint/config.yaml`, and `roles/**` in `DEFAULT_PATCH_GLOBS`. That means a future edit to any of these three files — including one that weakens the gate's own enforcement policy (e.g. hand-editing `.waypoint/config.yaml`'s `tiers.patch` to loosen what it enforces), or rewrites an agent's role-prompt instructions (`roles/*.md`) — requires no spec delta and isn't blocked by the gate, permanently, not just for the bootstrap commit. Unlike `tasks/**` (also patch-classified, but safe: `waypoint verify`'s `.gate-state` hash independently catches tampering with a ledger's completion claims), no other mechanism in this codebase protects `.waypoint/config.yaml` or `roles/**` from an unenforced, undelta'd rewrite.
  evidence: Surfaced by two independent review lenses during the fix for the fresh-install bootstrap-blocking bug (`epic-1-5-mvp-retro-2026-08-25.md`, Finding 1). The user was asked and explicitly chose to accept this tradeoff — narrowed from the initially wider fix to exclude `.gate-state/**` (by patch-classifying the literal `.waypoint/config.yaml` rather than the broader `.waypoint/**`) and to exclude `decisions/**` (dropped entirely, since `scaffold()` only ever creates an empty `decisions/` directory with nothing to commit from it) — rather than pursue a bigger redesign, e.g. having `waypoint install` commit its own scaffold directly so these files stay under normal enforcement. Worth revisiting if a future story wants stronger self-modification protection for the gate's own policy file specifically.
